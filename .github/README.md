## Secrets to Add in GitHub Repo Settings

In Settings → Secrets → Actions → New repository secret:

AWS_ACCESS_KEY_ID – IAM user with Terraform + Lambda deploy permissions

AWS_SECRET_ACCESS_KEY – Secret key for above

OPENAI_API_KEY – OpenAI API key (sk-xxxx)

JWT_SECRET – Secret used to sign/verify JWTs
